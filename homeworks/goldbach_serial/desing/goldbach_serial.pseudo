// Debe al menos tener las subrutinas de lectura, cálculo de sumas de Goldbach, 
// impresión, y subrutina principal. La del cálculo de las sumas probablemente 
// necesitará subdividirla. 


int main () {
  input_numeros = Leer(input.txt)


  input_mas_grande = largest_element_arr(&archivo)
  trial_division(&array_primos, &input_mas_grande)


  // Este calcula la cantidad de sumas
  Goldbach(&input_numeros, &array_primos)

  // Estos 2 se encargan de conseguir las distintas combinatorias
  Goldbach_par(&input_numeros, &array_primos)
  Goldbach_impar(&input_numeros, &array_primos)
}