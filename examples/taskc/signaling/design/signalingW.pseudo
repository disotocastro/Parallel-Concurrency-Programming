procedure main:
// we add a semaphore to give order to the solution
thread_count := read_interger("How many threads? ");
  create_threads(thread_count, secundary)
end procedure
//TODO: add the secundary procedure to be always in order
procedure secundary:
  statement a
end procedure
